在新分支 `compat/dsh-012` 上执行兼容修复，不修改或提交 main：

- 建立分支并确认干净基线。
- 核对并补齐 DSH 客户端实际依赖注入，兼容旧版 rc.7 与 0.1.2-rc.1。
- 增加 locale、slots、connection、remote 和 Settings slot 的能力探测及安全降级。
- 给 SkillsSection 增加局部错误边界和连接未就绪 fallback，避免白屏。
- 仅在核对到实际 API 差异时增加 Host skills/webServer 兼容适配。
- 补充回归测试并运行 typecheck、Vitest、构建和 pack check。
- 不提交、不推送。